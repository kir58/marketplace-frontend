import React from 'react';
import { Cart } from '@shared/features/cart';
import { Layout } from '@shared/widgets/layout';

export const CartPage: React.FC = () => (
  <Layout>
    <Cart />
  </Layout>
);
